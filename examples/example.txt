<div className="absolute bottom-10 
            left-1/2 -translate-x-1/2 hidden 
            group-hover:block bg-black
             text-white text-xs px-3 py-2
              rounded shadow whitespace-pre-line
               text-center">



className="absolute bottom-10 left-1/2 -translate-x-1/2 hidden group-hover:block 
             bg-black text-white text-xs px-3 py-2 rounded shadow text-center 
             w-full max-w-sm"


<div className="absolute left-1/2 -translate-x-1/2 top-full w-2 h-2 bg-black rotate-45"></div>

<div className="absolute left-1/2 -translate-x-1/2 top-full w-2 h-2 bg-[#FFFFFF] border-r border-b border-[#FFFFFF] rotate-45 shadow-[2px_2px_4px_#0000002A]"></div>


absolute left-1/2 -translate-x-1/2 -top-1.5 w-2 h-2 



<div className="flex flex-wrap gap-3 bg-gray-100 p-2 rounded-lg">
  {selectedFiles.map((file, idx) => (
    <div
      key={idx}
      className="flex items-center gap-2 p-2"
    >
      <div className="relative w-[60px] h-[60px]">
        {previews[idx] ? (
          <img
            src={previews[idx]}
            alt="preview"
            className="w-full h-full object-cover rounded"
          />
        ) : (
          <FileText size={32} className="text-gray-500 absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2" />
        )}

        {/* Circle X button on image corner */}
        <button
          className="absolute top-[-6px] right-[-6px] w-5 h-5 flex items-center justify-center 
                     bg-black text-white rounded-full hover:bg-gray-800"
          onClick={() => {
            setSelectedFiles((prev) => prev.filter((_, i) => i !== idx));
            setPreviews((prev) => prev.filter((_, i) => i !== idx));
          }}
        >
          <X size={12} />
        </button>
      </div>

    </div>
  ))}
</div>



<div className="relative w-[60px] h-[60px] overflow-hidden rounded group">
  <img
    src={previews[idx]}
    alt="preview"
    className="w-full h-full object-cover rounded group-hover:brightness-110 transition duration-300"
  />

  <button
    className="absolute top-[-6px] right-[-6px] w-5 h-5 flex items-center justify-center 
               bg-black text-white rounded-full hover:bg-gray-800"
    onClick={() => {
      setSelectedFiles((prev) => prev.filter((_, i) => i !== idx));
      setPreviews((prev) => prev.filter((_, i) => i !== idx));
    }}
  >
    <X size={12} />
  </button>
</div>


